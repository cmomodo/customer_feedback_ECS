# Terraform Bootstrap

This folder bootstraps the Terraform remote-state S3 bucket used by `infra/`.
we will use it to impement the chicken and egg principle. 
we will also use the script for the action secrets we need.
